name: Secrets
on: 
  workflow_dispatch:
    inputs:
      greeting:
        type: choice 
        options:
          - info
          - warn
          - error
jobs:
  print:
    runs-on: ubuntu-latest
    env:
      HOGE: ABCDEFG
    steps:  
      - run: echo "${HOGE:0:3} ${HOGE#??}" # "${HOGE:0:3}"の最初の3文字を取得、"${HOGE#??}"の最初の２文字を削除して出力
