name: test
on: 
  workflow_dispatch:
    inputs: 
      greeting:
        type: string
        default: hello
        required: true
        description: A cheerful word
jobs:
  test1:
    runs-on: ubuntu-latest
    steps:
      - run: |
         echo "1"
         echo "${{inputs.greeting}}"
         
  test2:
    runs-on: ubuntu-latest
    env:  
      BRANCH: ${{'main'}}       
    steps:
      - run: echo BRANCH
  
  test3:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{github.actor}}"
      

